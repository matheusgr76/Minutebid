# Minutebid Environment Template
# Copy this to .env and fill in your credentials.

# --- Telegram Bot ---
# 1. Message @BotFather on Telegram to create a bot and get a TOKEN.
# 2. Add your bot to a group or message it.
# 3. Message @userinfobot to get your CHAT_ID.
TELEGRAM_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# --- Polymarket CLOB (Automatic Betting) ---
# Generate these in Polymarket UI: Settings → API
# CLOB_PK is your wallet's private key (hex, without 0x prefix).
# WARNING: treat CLOB_PK as a secret — never commit it to git.
CLOB_PK=your_wallet_private_key_hex
CLOB_API_KEY=your_clob_api_key
CLOB_API_SECRET=your_clob_api_secret
CLOB_API_PASSPHRASE=your_clob_api_passphrase
